{% extends "::base.html.twig" %}

{% block body -%}
    {{ parent() }}
    <div>
        <a class="btn btnBeigeEltonGrand shadow bold" href="{{path('division')}}">Retour à la liste des classes</a>
        <br/><br/>
    </div>
    <div>
        <div  style="margin: 0px 0px 25px 0px;">
            <h1 class="label label-warning bold">&nbsp  Classe</h1>
        </div>

        <table class="breadcrumb">
            <tbody>
                <tr>
                    <th class="txtOrangeElton">Nom de la classe</th>
                    <td>{{ entity.libelle }}</td>
                </tr>
                <tr>
                    <th class="txtOrangeElton">Identifiant de la classe</th>
                    <td>{{ entity.username }}</td>
                </tr>
                <tr>
                    <th class="txtOrangeElton">Mot de passe de la classe</th>
                    <td>{{ entity.password }}</td>
                </tr>
                <tr>
                    <th class="txtOrangeElton">Nom du professeur</th>
                    <td>{{ entity.teacher.name}}</td>
                </tr>
                <tr>
                    <th class="txtOrangeElton">Niveau de la classe</th>
                    <td>{{ entity.level.libelle}}</td>
                </tr>
                <tr>
                    <th class="txtOrangeElton">Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>
                        <br/>
                        <a class="btn shadow center-block bold btnBeigeEltonPetit" data-toggle="modal" data-target="#myDivisionUpdateModal">Modifier</a>
                        {# Teacher edit modal #}
                        <div class="modal fade" id="myDivisionUpdateModal" tabindex="-1" role="dialog" aria-labelledby="myDivisionUpdateModalLabel" aria-hidden="true">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
                                        <h4 class="modal-title label label-warning modalFS" id="myDivisionUpdateModalLabel   ">Modification de classe</h4>
                                    </div>
                                    <div class="modal-body">
                                        {{render(controller('EltonDivisionBundle:Division:edit', {'id' : entity.id})) }}
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn center-block shadow btnBeigeElton bold" data-dismiss="modal">Fermer</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </th>
                    <td>
                        <br/>
                        {% form_theme delete_form 'EltonCoreBundle:Form:delete_field.html.twig' %}
                        {{ form(delete_form) }}
                    </td>
                </tr>
                <tr>
                    <div class="smallElton"></div>
                </tr>
            </tbody>
        </table>
    </div>
{% endblock %}