{% extends "::base.html.twig" %}

{% block body -%}
    {{ parent() }}
    <div class="modal fade" id="myUnactiveModal" tabindex="-1" role="dialog" aria-labelledby="myUnactiveModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
                    <h4 class="modal-title label label-warning modalFS" id="myUnactiveModalLabel">En construction !</h4>
                </div>
                <div class="modal-body" style="text-align:center;">
                    Ce cours sera accessible prochainement
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn center-block shadow btnBeigeElton orange bold" style="color:white!important;"data-dismiss="modal">Fermer</button>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                <h1 class="lessonCategoryTitle">{{entity.libelle}}</h1>
                <div class="buttonTableCont">
                        {% for lesson in entity.lessons %}
                            {% if lesson.active %}
                                <a class="buttonTableSmall {{lesson.type}}" href="{{path('lesson_show', { id : lesson.id, order : loop.index })}}"> <span class="lessonNumber">{{loop.index}}</span> <br/> {{lesson.libelle}}</a>
                            {% else %}
                                <a class="buttonTableSmall  {{lesson.type}}" style="opacity: 0.5;" data-toggle="modal" data-target="#myUnactiveModal"><span class="lessonNumber">{{loop.index}}</span> <br/> {{lesson.libelle}}</a>
                            {% endif %}
                            {% if loop.index % 3 == 0 %}
                            </div>
                            <div class="buttonTableCont">
                            {% endif %}
                        {% endfor %}
                </div>
            </div>
            </div>
        </div>
    </div>
{% endblock %}