{% extends "::base.html.twig" %}
{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="row" style="margin-top: 60px;">
            <div class=" buttonTableCont ">
                    <a class="buttonTable bleu fs18" href="{{path('monthly_event')}}">Évènement du mois</a>
                    <a class="buttonTable culture fs18" href="{{path('cultural_event')}}">Fiches culturelles</a>
            </div>
        </div>
    </div>
{% endblock %}